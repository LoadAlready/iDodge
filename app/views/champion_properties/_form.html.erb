<%= form_with(model: champion_property, local: true) do |form| %>
  <% if champion_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(champion_property.errors.count, "error") %> prohibited this champion_property from being saved:</h2>

      <ul>
      <% champion_property.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
